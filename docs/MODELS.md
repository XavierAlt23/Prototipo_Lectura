# Modelos

Modelos previstos:

- BETO, DistilBETO y BERT-M para experimentos transformer.
- BiLSTM y Conv1D-LSTM para redes recurrentes.
- CNN para clasificacion convolucional de texto.

Los checkpoints entrenados se guardan en `ml/checkpoints`.
